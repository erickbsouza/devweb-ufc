<div class="colum-4">
    <% if (pageData.loginFailed == 1) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert"">
            Usuário ou senha incorretos 
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button></div> 
    <% } %> 
</div>

<div id="card-container" class="card-container">
    <p id="loading-occurrence-message" class="card-title">Carregando dados, aguarde...</p>
</div>

<script src="/js/occurrence.js"></script>

<script>
    occurrences = JSON.parse('<%-JSON.stringify(searchedOccurrences)%>');
    window.onload = () => {
        updateChangeThemeButton();
        updateFontCss();
        document.getElementById("loading-occurrence-message").remove();
        mountOccurrenceHtml(occurrences);
    };
</script>